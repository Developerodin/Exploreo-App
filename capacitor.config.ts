import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.Exploro.App',
  appName: 'EXPLOREO',
  webDir: 'build',
  server: {
    androidScheme: 'https'
  }
};

export default config;
